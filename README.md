To run this app 
we need sql , sql workbench installed and configure the username password from application.properties file with the workbench 
once it is set check for table creation property in application.property file . and the run the app.
it should work
